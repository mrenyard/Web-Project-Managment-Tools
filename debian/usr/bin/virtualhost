#!/bin/bash -eu

declare -r USAGE_STATEMENT="Usage: virtualhost \"PROJECT_NAME\" remove|[add DOMAIN[ --from-manifest]]";
if [[ "$#" -lt 2 ]]; then echo ${USAGE_STATEMENT}; exit 1; fi

PROJECT_NAME=${1};
declare -r SWITCH=${2};
declare -r PROJECT_NAME=`echo ${PROJECT_NAME} | sed 's/"//g'`;

HOST=`echo "${PROJECT_NAME}" | sed 's/ //g' | sed 's/\//-/g'`;
declare -r HOST="${HOST,,}";

if [ $USER == 'root' ]; then USER=$SUDO_USER; HOME="/home/${SUDO_USER}"; fi
if [[ "${HOME}" == "/root" || "${HOME}" == "/home/root" ]]; then
  echo "${HOME} IS AN INVALID HOME PATH!";
  read -p "Enter username: " USERNAME;
  HOME=`echo "/home/${USERNAME}"`;
fi
export HOME;

case ${SWITCH} in
  "add")
    if [[ "$#" -gt 2 ]]; then
      declare -r DOMAIN=${3};
    else
      echo -e "  ...\e[0;31mFAILED!\e[0m DOMAIN requiered!";
      exit 2;
    fi
    FULL_DOMAIN=`get-full-domain ${DOMAIN}`;

    UPDATE_MANIFEST=true;
    if [[ "$#" -gt 3 ]]; then
     if [ ${4} != "--from-manifest" ]; then echo ${USAGE_STATEMENT}; exit 1; fi
     UPDATE_MANIFEST=false;
    fi

    sudo echo "SETTING UP NEW VIRTUAL HOST: ${HOST}";
    if [[ ! -L /var/www/${HOST} ]]; then
      echo "  ADDING symbolic links";
      echo "    ${HOST}"; sudo /bin/ln -s "${HOME}/Projects/${PROJECT_NAME}/www" "/var/www/${HOST}";
      echo "    style.${HOST}"; sudo /bin/ln -s "${HOME}/Projects/${PROJECT_NAME}/www/assets/style" "/var/www/style.${HOST}";
      echo "    func.${HOST}"; sudo /bin/ln -s "${HOME}/Projects/${PROJECT_NAME}/www/assets/func" "/var/www/func.${HOST}";
      echo "    media.${HOST}"; sudo /bin/ln -s "${HOME}/Projects/${PROJECT_NAME}/www/assets/media" "/var/www/media.${HOST}";
      echo "    dev.${HOST}"; sudo /bin/ln -s "${HOME}/Projects/${PROJECT_NAME}/www" "/var/www/dev.${HOST}";
    fi
    if [[ ! -f /etc/apache2/sites-available/${HOST} ]]; then
      echo "  ADDING virtualhost file: '/etc/apache2/sites-available/${HOST}.conf'";
      sudo /bin/cp /etc/webshop/skel/vhost/base.txt /etc/apache2/sites-available/${HOST}.conf;
      echo "virtualhost-edit ${HOST} ${FULL_DOMAIN};";
      sudo virtualhost-edit ${HOST} ${FULL_DOMAIN};
    fi
    if $UPDATE_MANIFEST; then
      echo "  UPDATING MANIFEST";
      echo "${HOST} \"${PROJECT_NAME}\"" >> ${HOME}/Projects/.manifest;
    fi
    sudo chown -R www-data:www-data /var/www/*;
    sudo a2ensite ${HOST}.conf;
    echo "NEW VIRTUAL HOST SETUP COMPLETE!";
    ;;

  "remove")
    sudo echo "REMOVING VIRTUAL HOST: ${HOST}";
    sudo a2dissite ${HOST};
    if [[ -L /var/www/${HOST} ]]; then
      echo "  REMOVING symbolic links";
      sudo rm /var/www/*${HOST};
    fi
    if [[ -f /etc/apache2/sites-available/${HOST}.conf ]]; then
      echo "  REMOVING virtualhost file: '/etc/apache2/sites-available/${HOST}.conf'";
      sudo rm /etc/apache2/sites-available/${HOST}.conf;
    fi
    echo "  UPDATING MANIFEST";
    /bin/sed -i "/${HOST} \"${PROJECT_NAME}\"/d" ${HOME}/Projects/.manifest
    echo "VIRTUAL HOST REMOVAL COMPLETE!";
    ;;

  *)
    echo ${USAGE_STATEMENT}; exit 1;
    ;;
esac

sudo systemctl reload apache2.service;
exit 0;
